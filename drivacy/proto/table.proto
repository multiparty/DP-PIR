// Copyright 2020 multiparty.org

// Protobuf defining the format of the input table.

syntax = "proto3";

package drivacy.proto;

message Table {
  repeated Row rows = 1;
}

message Row {
  uint64 key = 1;
  uint64 value = 2;
}
